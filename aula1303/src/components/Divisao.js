import React from 'react'

const Divisao = (props) => {
  return (
    <div>
        <h1>A divisão de {props.num20} / {props.num2} é igual a {props.num20 / props.num2}</h1>
    </div>
  )
}

export default Divisao